{
  "name": "alpha-panel",
  "version": "5.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:remote": "wrangler d1 migrations apply alpha-db --remote"
  },
  "devDependencies": {
    "wrangler": "4.138.0"
  }
}
